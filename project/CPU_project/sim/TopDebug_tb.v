`timescale 1ns / 1ps
//============================================================================
// TopDebug_tb.v - 顶层仿真 Testbench
// 功能：对 TopDebug 进行功能仿真验证
//============================================================================

module TopDebug_tb();

    reg         clk;
    reg         rst;
    reg  [23:0] switch2N4;
    wire [23:0] led2N4;

    // 实例化被测模块
    TopDebug uut(
        .clk(clk),
        .rst(rst),
        .switch2N4(switch2N4),
        .led2N4(led2N4)
    );

    // 时钟生成：10ns 周期（100MHz）
    initial clk = 0;
    always #5 clk = ~clk;

    // 测试流程
    initial begin
        // 初始化
        rst = 1;
        switch2N4 = 24'b0;

        // 复位释放
        #100;
        rst = 0;

        // ================================================================
        // 用例0：AND 运算
        // TODO: 设置拨码开关输入值，等待 CPU 执行完毕，检查 LED 输出
        // ================================================================
        #200;
        // switch2N4 = 24'h??????;
        // #1000;
        // $display("Test0 AND: LED = %h", led2N4);

        // ================================================================
        // 用例1：SLL 运算
        // ================================================================
        // TODO

        // ================================================================
        // 用例2：SRA 运算
        // ================================================================
        // TODO

        // ================================================================
        // 用例3：LUI + ADD
        // ================================================================
        // TODO

        // ================================================================
        // 用例6：斐波那契数列
        // ================================================================
        // TODO

        // ================================================================
        // 用例7：popcount（统计1的个数）
        // ================================================================
        // TODO

        // ================================================================
        // 用例8：IEEE754 浮点数类型判断
        // ================================================================
        // TODO

        // ================================================================
        // 用例9：浮点数量化（16bit float → Q3.4 定点数）
        // ================================================================
        // TODO

        // 等待足够时间后结束仿真
        #10000;
        $display("=== Simulation Complete ===");
        $finish;
    end

    // 监控关键信号
    initial begin
        $monitor("Time=%0t PC=%h Instr=%h ALU=%h LED=%h",
                 $time,
                 uut.pc,
                 uut.instruction,
                 uut.alu_result,
                 led2N4);
    end

endmodule
